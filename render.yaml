services:
  - type: web
    name: ephemeral-chat-backend
    plan: free
    env: java
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/ephemeral-anonymous-chat-backend-0.0.1-SNAPSHOT.jar
